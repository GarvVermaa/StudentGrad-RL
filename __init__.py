# Root package — intentionally empty.
# Do NOT put imports here: the OpenEnv validator runs static analysis on
# every __init__.py before it resolves entry points, and a failed import
# at this level will report "main() not callable" instead of the real error.
