# server package — intentionally empty.
# Do NOT import StudentEnvironment here. The validator does a static-analysis
# pass on this file; any ImportError surfaces as "main() not callable"
# rather than the actual error, making it very hard to debug.
# Import StudentEnvironment directly where you need it (server/app.py already does).
